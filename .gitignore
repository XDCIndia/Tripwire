node_modules
.env
coverage
coverage.json
typechain
.devcontainer
out
.vscode

#Hardhat files
cache
artifacts
deployments

typechain-types
yarn-error.log

# Generated by deployment scripts - local-only, regenerate anytime
local-deployment.json
deployment.json

# Foundry (issue #100) — lib/ must NEVER be inside contracts/
# If contracts/lib/ exists, Hardhat compilation breaks (HH411).
# foundry.toml is at the project root so forge install puts lib/ at root.
forge-out
lib/
contracts/lib/
